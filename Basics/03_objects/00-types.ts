// ============================================
// 1. EXPORT TYPES
// ============================================

/* TypeScript tiene su propio sistema de modulos, basado en JavaScript ES6. */

export type User = {
  id: number;
  username: string;
  email: string;
  isDeveloper: boolean;
}